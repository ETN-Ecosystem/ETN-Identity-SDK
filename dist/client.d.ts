export interface ETNAuthClientConfig {
    clientId: string;
    redirectUri: string;
    scope?: string;
}
export declare class ETNAuthClient {
    private clientId;
    private redirectUri;
    private scope;
    private static readonly ENDPOINTS;
    constructor(config: ETNAuthClientConfig);
    /**
     * Constructs the OIDC Authorization URL for the "Sign in with ETN" flow.
     *
     * @param state - A random string generated by the app to prevent CSRF.
     * @returns The fully constructed URL to redirect the user to.
     */
    buildAuthorizeUrl(state?: string): string;
}
